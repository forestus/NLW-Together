{
    "type": "postgres",
    "host": "localhost",
    "port": "5432",
    "username": "maciel",
    "password": "852456",
    "database": "postgres",
    "logging": false,
    "migrations": [
        "src/migrations/*.{js,ts}"
    ],
    "entities": [
        "src/entities/*.{js,ts}"
    ],
    "cli": {
        "migrationsDir": "src/migrations",
        "entitiesDir": "src/entities"
    }
}